defaults:
  - data: default
  - model: default
  - _self_

train:
  max_epochs: 50
  accelerator: auto
  devices: 2                # number of GPUs
  strategy: ddp             # DistributedDataParallel
  precision: 16-mixed
  log_every_n_steps: 10

wandb:
  project: nih-chest-xray
  name: null   # auto-generated if null